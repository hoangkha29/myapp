%h1.blog-title= @article.title
.main
  %article
    %div.post-meta
      %i.updateTime= @article.created_at.strftime("%b %d,%Y")
    %p{:style => "margin-bottom: 55px;"}= @article.body
    = button_to "Edit", edit_article_path(@article) , method: :get, :class => "btn btn-primary"
    = button_to "Delete", @article , method: :delete, :class => "btn btn-danger", data: { confirm: "Are you sure?" }
  %br/
  %br/
  %br/

  -#%select.country
  -#  %option{value: "vn", selected: "Selected"} VN
  -#  %option{value: "sg"} SG
  -#  %option{value: "fr"} FR
  -#  %option{value: "us"} US
  -#-# = image_tag ('flags/vn.png')
  -#%span.flag


  .comment
    %h2 Comments
    .div-create= render @article.comments
    %h3 Add a comment:
    .ajax-form-create= render 'comments/form'

